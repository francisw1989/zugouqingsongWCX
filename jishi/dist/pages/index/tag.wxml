<!--pages/index/tag.wxml-->
<view class=' bor_btm_so'>
    <ui-tabs index="{{ current }}" bindchange="handleChange" class="bgfff" ink-bar="true" ink-bar-style="{{inkBarStyle}}" _pid="{{_pid}}">
        <ui-tab wx:for="{{tabs}}" _pid="{{_pid}}">{{item}}</ui-tab>
    </ui-tabs>
    <view class=" pad10">
        <view class="btns btns-small btns-green-bor" style="margin: 10rpx" wx:for='{{list}}'>红茶</view>
    </view>
    
</view>
<view class='align-right pad15 top10'>
        <view class='btns btns-green btns-small' bindtap='confirm'>保存</view>
    </view>
